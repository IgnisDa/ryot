# TODO: delete this file eventually

services:
  ryot-redis:
    image: redis:alpine
    restart: unless-stopped
    ports:
      - "6380:6379"
    healthcheck:
      retries: 5
      timeout: 5s
      interval: 2s
      test: ["CMD", "redis-cli", "ping"]

  ryot-bull-board:
    restart: unless-stopped
    image: venatum/bull-board
    ports:
      - "3002:3000"
    environment:
      REDIS_HOST: ryot-redis
    depends_on:
      ryot-redis:
        condition: service_healthy

  ryot-postgres:
    restart: unless-stopped
    image: postgres:18-alpine
    command: postgres -c shared_preload_libraries=pg_stat_statements -c wal_level=logical
    ports:
      - "5432:5432"
    volumes:
      - ryot_postgres_storage:/var/lib/postgresql
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      retries: 5
      timeout: 5s
      interval: 2s
      test: ["CMD-SHELL", "pg_isready -U postgres"]

  ryot:
    restart: unless-stopped
    ports:
      - "8000:8000"
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      ryot-redis:
        condition: service_healthy
      ryot-postgres:
        condition: service_healthy
    environment:
      FRONTEND_URL: http://localhost:8000
      REDIS_URL: redis://ryot-redis:6379
      SERVER_ADMIN_ACCESS_TOKEN: super-secret-token-that-should-be-changed
      DATABASE_URL: postgresql://postgres:postgres@ryot-postgres:5432/postgres
      FILE_STORAGE_LOCAL_DIR: /home/ryot/storage
      FILE_STORAGE_LOCAL_TEMP_DIR: /home/ryot/work
      FILE_STORAGE_LOCAL_SIGNING_SECRET: change-this-local-signing-secret
    volumes:
      - ryot_local_storage:/home/ryot/storage
    tmpfs:
      - /home/ryot/work:uid=1001,gid=1001,mode=1770

volumes:
  ryot_postgres_storage:
  ryot_local_storage:
