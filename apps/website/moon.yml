layer: application

dependsOn:
  - "assets"

tasks:
  dev:
    command: "react-router dev --port 5200 --host"
    local: true

  build:
    command: "react-router build"
    inputs: ["**/*.{ts,tsx}"]
    outputs: ["build"]

  typecheck:
    inputs: ["**/*.{ts,tsx}"]
