layer: application

dependsOn:
  - "assets"

tasks:
  dev:
    command: "react-router dev --port 3000 --host"
    local: true

  build:
    command: "react-router build"
    inputs: ["@group(source)"]
    outputs: ["build"]

  typecheck:
    inputs: ["@group(source)"]
