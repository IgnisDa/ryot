import { SettingsPlaceholderScreen } from "@/modules/settings/settings-placeholder-screen";

export default function ImportDataSettings() {
	return (
		<SettingsPlaceholderScreen
			title="Import data"
			detail="TODO: Start manual imports and review import progress, failures, and results."
		/>
	);
}
