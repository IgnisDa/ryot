fragment MediaPart on Media {
  id
  title {
    userPreferred
  }
  episodes
  chapters
  type
  description
  coverImage {
    extraLarge
  }
  bannerImage
  staff {
    edges {
      node {
        id
        name {
          full
        }
      }
      role
    }
  }
}

query SearchQuery($search: String!, $page: Int!, $type: MediaType!) {
  Page(page: $page, perPage: 20) {
    pageInfo {
      total
    }
    media(search: $search, type: $type) {
      ...MediaPart
    }
  }
}
