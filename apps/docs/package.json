{
	"name": "@ryot/docs",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vitepress build",
		"dev": "vitepress dev --port 3002",
		"check": "tsc && oxfmt . --write && oxlint . --type-aware --fix"
	},
	"devDependencies": {
		"vitepress": "2.0.0-alpha.15"
	}
}
