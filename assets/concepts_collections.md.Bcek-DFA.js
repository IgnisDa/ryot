import{_ as o,c as t,o as a,aj as i}from"./chunks/framework.lC5GZWV9.js";const h=JSON.parse('{"title":"Collections","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/collections.md","filePath":"concepts/collections.md","lastUpdated":1772193071000}'),n={name:"concepts/collections.md"};function s(d,e,l,c,r,m){return a(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="collections" tabindex="-1">Collections <a class="header-anchor" href="#collections" aria-label="Permalink to “Collections”">​</a></h1><p>Collections are one of the core organizing systems in Ryot. They are not only labels for your media: they also drive dashboard sections, notification behavior, and some automatic status transitions.</p><h2 id="default-collections" tabindex="-1">Default Collections <a class="header-anchor" href="#default-collections" aria-label="Permalink to “Default Collections”">​</a></h2><p>Ryot creates these collections automatically for every user account. They are default system collections and cannot be deleted.</p><table tabindex="0"><thead><tr><th>Collection</th><th>Description</th><th>Special behavior</th></tr></thead><tbody><tr><td><strong>Watchlist</strong></td><td>Items you want to consume in the future</td><td>Automatically removed when progress/seen is updated</td></tr><tr><td><strong>In Progress</strong></td><td>Items you are currently consuming</td><td>Powers the &quot;In Progress&quot; dashboard section</td></tr><tr><td><strong>Completed</strong></td><td>Items you have finished</td><td>Used for completion history and filters</td></tr><tr><td><strong>Monitoring</strong></td><td>Items you want to keep an eye on</td><td>Powers upcoming events and update notifications</td></tr><tr><td><strong>Owned</strong></td><td>Items in your physical/digital inventory</td><td>Includes an optional <code>Owned on</code> date field</td></tr><tr><td><strong>Reminders</strong></td><td>Items with scheduled reminders</td><td>Uses reminder-specific extra fields</td></tr><tr><td><strong>Custom</strong></td><td>Items you created manually in Ryot</td><td>Custom metadata/groups/people are added here automatically</td></tr></tbody></table><h2 id="automatic-collection-management" tabindex="-1">Automatic Collection Management <a class="header-anchor" href="#automatic-collection-management" aria-label="Permalink to “Automatic Collection Management”">​</a></h2><p>Ryot applies collection automation when progress/seen state changes are recorded.</p><h3 id="step-1-watchlist-removal" tabindex="-1">Step 1: Watchlist removal <a class="header-anchor" href="#step-1-watchlist-removal" aria-label="Permalink to “Step 1: Watchlist removal”">​</a></h3><p>On every progress/seen update, Ryot first removes the item from <code>Watchlist</code> (if present).</p><h3 id="step-2-state-based-rules" tabindex="-1">Step 2: State-based rules <a class="header-anchor" href="#step-2-state-based-rules" aria-label="Permalink to “Step 2: State-based rules”">​</a></h3><p>When state becomes <code>In Progress</code>:</p><ol><li>Add item to <code>In Progress</code></li><li>Add item to <code>Monitoring</code></li></ol><p>When state becomes <code>Dropped</code> or <code>On Hold</code>:</p><ol><li>Remove item from <code>In Progress</code></li><li>Leave <code>Monitoring</code> unchanged</li></ol><p>When state becomes <code>Completed</code>, behavior depends on media type:</p><ul><li><strong>Non-episodic media</strong> (movies, books, audiobooks, music, video games, comic books, visual novels): <ol><li>Add to <code>Completed</code></li><li>Remove from <code>In Progress</code></li><li>Remove from <code>Monitoring</code></li></ol></li><li><strong>Episodic media</strong> (shows, anime, manga, podcasts): <ul><li>Use the completion algorithm below</li><li>If complete: add to <code>Completed</code>, remove from <code>In Progress</code></li><li>If not complete: keep/add in <code>In Progress</code> and <code>Monitoring</code></li></ul></li></ul><div class="info custom-block"><p class="custom-block-title custom-block-title-default">INFO</p><p>For episodic media, <code>Monitoring</code> is not auto-removed on completion. This helps ongoing series continue surfacing future updates.</p></div><h2 id="episodic-completion-algorithm" tabindex="-1">Episodic Completion Algorithm <a class="header-anchor" href="#episodic-completion-algorithm" aria-label="Permalink to “Episodic Completion Algorithm”">​</a></h2><p>For shows, anime, manga, and podcasts, Ryot decides completion using consume counts.</p><h3 id="core-rule" tabindex="-1">Core rule <a class="header-anchor" href="#core-rule" aria-label="Permalink to “Core rule”">​</a></h3><p>All tracked episodes/chapters must have equal, non-zero consume counts.</p><p>Example for a 10-episode show:</p><ul><li>Episodes 1-10 watched once each -&gt; <strong>Completed</strong></li><li>Episodes 1-9 watched once, episode 10 watched twice -&gt; <strong>Not completed</strong></li><li>Episodes 1-10 watched twice each -&gt; <strong>Completed</strong> (second full pass)</li></ul><p>This is why rewatching a single favorite episode can put a show back in <code>In Progress</code> until other episodes catch up.</p><h3 id="specials-and-extras-exclusion" tabindex="-1">Specials and extras exclusion <a class="header-anchor" href="#specials-and-extras-exclusion" aria-label="Permalink to “Specials and extras exclusion”">​</a></h3><p>For shows, seasons named <code>Specials</code> or <code>Extras</code> are excluded from completion counting.</p><h3 id="unknown-totals" tabindex="-1">Unknown totals <a class="header-anchor" href="#unknown-totals" aria-label="Permalink to “Unknown totals”">​</a></h3><p>If Ryot cannot determine a full episode/chapter set (common with ongoing anime/manga), it treats the item as complete by default. Strict counting starts once totals become known.</p><h2 id="show-update-edge-case-completed-watchlist" tabindex="-1">Show Update Edge Case: Completed -&gt; Watchlist <a class="header-anchor" href="#show-update-edge-case-completed-watchlist" aria-label="Permalink to “Show Update Edge Case: Completed -&gt; Watchlist”">​</a></h2><p>There is one additional automation path for shows during metadata refresh.</p><p>If a show is in both <code>Completed</code> and <code>Monitoring</code>, and provider updates add new not-yet-seen content (for example new episodes/seasons), Ryot can:</p><ol><li>Remove it from <code>Completed</code></li><li>Add it to <code>Watchlist</code></li><li>Send a notification about the move</li></ol><p>This helps re-surface shows that were previously complete.</p><h2 id="monitoring-and-notifications" tabindex="-1">Monitoring and Notifications <a class="header-anchor" href="#monitoring-and-notifications" aria-label="Permalink to “Monitoring and Notifications”">​</a></h2><p><code>Monitoring</code> is the main driver for update-based awareness in Ryot.</p><ul><li>The dashboard <code>Upcoming</code> section is built from monitored items with upcoming calendar events</li><li>Monitoring notifications include more than episode releases, such as metadata status changes, release-date changes, and episode/chapter count changes</li></ul><h2 id="reminders-behavior" tabindex="-1">Reminders Behavior <a class="header-anchor" href="#reminders-behavior" aria-label="Permalink to “Reminders Behavior”">​</a></h2><p>The default <code>Reminders</code> collection uses reminder-specific data fields (date + text). On the scheduled date, Ryot sends a reminder notification and removes the item from <code>Reminders</code>.</p><h2 id="manual-collection-management" tabindex="-1">Manual Collection Management <a class="header-anchor" href="#manual-collection-management" aria-label="Permalink to “Manual Collection Management”">​</a></h2><p>You can always add or remove collection memberships manually from an item page.</p><p>Common use cases:</p><ul><li>Keep manual control when algorithmic completion is not what you want</li><li>Remove from <code>In Progress</code> when you decide not to continue</li><li>Keep in <code>In Progress</code> but remove <code>Monitoring</code> to reduce notifications</li></ul><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>Notifications are controlled by <code>Monitoring</code>, not <code>Completed</code>. Adding something to <code>Completed</code> does not disable update notifications by itself.</p></div><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>If you update progress again later, automation rules may add <code>In Progress</code>/<code>Monitoring</code> back, depending on the new state.</p></div><h2 id="dashboard-mapping" tabindex="-1">Dashboard Mapping <a class="header-anchor" href="#dashboard-mapping" aria-label="Permalink to “Dashboard Mapping”">​</a></h2><ul><li><strong>In Progress</strong> section shows items in the <code>In Progress</code> collection</li><li><strong>Upcoming</strong> section is driven by monitored items that have upcoming events</li></ul>',46)])])}const u=o(n,[["render",s]]);export{h as __pageData,u as default};
