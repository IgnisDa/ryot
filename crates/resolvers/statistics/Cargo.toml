[package]
name = "statistics-resolver"
version = "0.1.0"
edition = "2024"

[dependencies]
async-graphql = { workspace = true }

common-models = { workspace = true }
dependent-models = { path = "../../models/dependent" }
statistics-service = { path = "../../services/statistics" }
traits = { path = "../../traits" }
