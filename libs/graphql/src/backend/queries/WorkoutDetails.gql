query WorkoutDetails($workoutId: String!) {
  workoutDetails(workoutId: $workoutId) {
    id
    name
    endTime
    startTime
    templateId
    repeatedFrom
    summary {
      ...WorkoutSummaryPart
    }
    information {
      ...WorkoutInformationPart
    }
  }
}
