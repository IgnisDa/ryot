query SeenHistory($metadataId: Int!) {
  seenHistory(metadataId: $metadataId) {
    id
    progress
		state
    startedOn
    finishedOn
    lastUpdatedOn
		showInformation {
			episode
			season
		}
		podcastInformation {
			episode
		}
  }
}
