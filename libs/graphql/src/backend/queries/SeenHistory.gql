query SeenHistory($metadataId: Int!) {
  seenHistory(metadataId: $metadataId) {
    id
    progress
		dropped
    startedOn
    finishedOn
    lastUpdatedOn
		showInformation {
			episode
			season
		}
		podcastInformation {
			episode
		}
  }
}
