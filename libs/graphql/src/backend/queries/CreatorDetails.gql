query CreatorDetails($creatorId: Int!) {
  creatorDetails(creatorId: $creatorId) {
    details {
      id
      name
      image
    }
    contents {
      name
      items {
        metadataId
        title
        image
      }
    }
  }
}
