query ExerciseDetails($exerciseId: Int!) {
  exerciseDetails(exerciseId: $exerciseId) {
    name
    lot
    source
    level
    force
    mechanic
    equipment
    muscles
    attributes {
      instructions
      images
    }
  }
}
