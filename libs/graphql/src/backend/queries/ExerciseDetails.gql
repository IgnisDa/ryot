query ExerciseDetails($exerciseId: String!) {
  exerciseDetails(exerciseId: $exerciseId) {
    id
    lot
    source
    level
    force
    mechanic
    equipment
    muscles
    attributes {
      instructions
      images
    }
  }
}
