query ExercisesList($input: ExercisesListInput!) {
  exercisesList(input: $input) {
    total
    nextPage
    items {
      id
      name
      attributes {
        force
        level
        mechanic
        equipment
        primaryMuscles
        secondaryMuscles
        category
        instructions
        images
      }
    }
  }
}
