query MetadataGroupDetails($metadataGroupId: Int!) {
  metadataGroupDetails(metadataGroupId: $metadataGroupId) {
    details {
      id
      title
      lot
      displayImages
      parts
    }
    contents {
      item {
        identifier
        title
        image
      }
      part
    }
  }
}
