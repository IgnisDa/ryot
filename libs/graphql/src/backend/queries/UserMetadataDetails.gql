query UserMetadataDetails($metadataId: Int!) {
  userMetadataDetails(metadataId: $metadataId) {
    collections {
      ...CollectionPart
    }
    inProgress {
      ...SeenPart
    }
    history {
      ...SeenPart
    }
    averageRating
    unitsConsumed
    reviews {
      ...ReviewItemPart
    }
    seenBy
    nextEntry {
      season
      episode
      chapter
    }
  }
}
