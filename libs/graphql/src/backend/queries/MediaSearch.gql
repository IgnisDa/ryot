query MediaSearch($lot: MetadataLot!, $input: SearchInput!) {
  mediaSearch(lot: $lot, input: $input) {
    total
    nextPage
    items {
      identifier
      title
      images
      publishYear
    }
  }
}
