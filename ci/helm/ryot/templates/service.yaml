apiVersion: v1
kind: Service
metadata:
  name: {{ include "ryot.fullname" . }}
  labels:
    {{- include "ryot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ryot.selectorLabels" . | nindent 4 }}
